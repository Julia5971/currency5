// src/services/ExchangeRateService.js

/**
 * 지금은 테스트를 통과시키기 위한 임시 함수입니다.
 * 나중에 실제 API를 호출하는 로직으로 대체될 것입니다.
 */
export const fetchExchangeRates = async () => {
    return {
      base: 'USD',
      rates: {
        KRW: 1350.25, // 테스트를 통과시킬 임의의 숫자
      },
    };
  };